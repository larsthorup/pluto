/*global define*/
define(function (require) {
    'use strict';
    var Backbone = require('backbone');

    var CardsView = Backbone.View.extend({

        events: function () {
            return {

            };
        },

        initialize: function () {

        },

        render: function () {

        }
    });
    return CardsView;
});